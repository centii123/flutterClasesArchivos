import 'package:flutter/material.dart';

class videoCard extends StatelessWidget {
  const videoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hola videoCard'),
    );
  }
}